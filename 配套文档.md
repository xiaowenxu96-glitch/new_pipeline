# 数据管道配套文档

> 项目名称：new_pipeline（申万宏源行业数据自动化管道）
> 生成日期：2026-07-01
> 架构模式：配置驱动四层架构（Config → Core Engine → Plugin → Runner）

---

## 一、文件公共模块说明

### 1.1 PipelineEngine（[core_engine/action_dispatcher.py](core_engine/action_dispatcher.py)）

| 项目 | 说明 |
|------|------|
| **功能** | 核心管道引擎。负责加载 YAML 配置、预加载源数据、创建备份、按配置逐 sheet 执行 action 管道（actions → post_processes → finalize_actions）、保存输出文件。维护全局 action 注册表，将字符串 action 类型映射到各 plugin 的静态方法。 |
| **入参** | `config_path: str` — YAML 配置文件路径 |
| **出参** | 无返回值（None），将处理结果写入目标 Excel 文件并保存 |
| **可复用范围** | 所有行业通用。通过加载不同 YAML 配置文件驱动不同行业的处理流程 |
| **依赖限制** | 依赖 openpyxl、yaml、pandas；依赖 `core_engine/data_reader.py`、`core_engine/transformers.py`；依赖 `plugins/` 目录下各行业插件模块 |
| **核心方法** | `__init__(config_path)` — 加载配置、构建 action 注册表；`run()` — 执行完整 Pipeline：预加载数据 → 创建备份 → 逐 sheet 遍历 → 执行 actions → 执行 post_processes → save → 执行 finalize_actions |

**Action 注册表（当前已注册的完整映射）：**

| Action 类型 | 目标方法 | 所属行业 |
|------------|---------|---------|
| `apply_formula` | `apply_formula()` | 通用 |
| `aviation_write_airline_sheet` | `AviationPlugin.aviation_write_airline_sheet` | 航空 |
| `aviation_apply_yoy_formulas` | `AviationPlugin.aviation_apply_yoy_formulas` | 航空 |
| `aviation_apply_yoy_diff_formulas` | `AviationPlugin.aviation_apply_yoy_diff_formulas` | 航空 |
| `aviation_apply_yoy19_formulas` | `AviationPlugin.aviation_apply_yoy19_formulas` | 航空 |
| `aviation_apply_diff19_formulas` | `AviationPlugin.aviation_apply_diff19_formulas` | 航空 |
| `aviation_clear_early_years_data` | `AviationPlugin.aviation_clear_early_years_data` | 航空 |
| `aviation_adjust_format_after_full_year` | `AviationPlugin.aviation_adjust_format_after_full_year` | 航空 |
| `aviation_clear_ytd_2018_diff_data` | `AviationPlugin.aviation_clear_ytd_2018_diff_data` | 航空 |
| `aviation_clear_ax_ay_az_columns` | `AviationPlugin.aviation_clear_ax_ay_az_columns` | 航空 |
| `aviation_write_monthly_report_header_info` | `AviationPlugin.aviation_write_monthly_report_header_info` | 航空 |
| `aviation_write_quarterly_report_header_info` | `AviationPlugin.aviation_write_quarterly_report_header_info` | 航空 |
| `aviation_write_report_data` | `AviationPlugin.aviation_write_report_data` | 航空 |
| `write_indicator_group` | `MacroPlugin.macro_write_indicator_group` | 宏观 |
| `create_pivot_table` | `MacroPlugin.macro_create_pivot_table` | 宏观 |
| `macro_create_festival_pivot` | `MacroPlugin.macro_create_festival_pivot` | 宏观 |
| `macro_create_chuxi_pivot` | `MacroPlugin.macro_create_chuxi_pivot` | 宏观 |
| `macro_create_weekly_pivot` | `MacroPlugin.macro_create_weekly_pivot` | 宏观 |
| `macro_create_yearly_date_scaffold` | `MacroPlugin.macro_create_yearly_date_scaffold` | 宏观 |
| `electronics_write_sheet` | `ElectronicsPlugin.electronics_write_sheet` | 电子 |
| `electronics_update_chart_ranges` | `ElectronicsPlugin.electronics_update_chart_ranges` | 电子 |
| `baijiu_write_sheet` | `BaijiuPlugin.baijiu_write_sheet` | 白酒 |
| `baijiu_write_sheet_by_code` | `BaijiuPlugin.baijiu_write_sheet_by_code` | 白酒 |
| `baijiu_fill_summary_rows` | `BaijiuPlugin.baijiu_fill_summary_rows` | 白酒 |
| `baijiu_fill_zuotu` | `BaijiuPlugin.baijiu_fill_zuotu` | 白酒 |
| `baijiu_finalize_charts` | `BaijiuPlugin.baijiu_finalize_charts` | 白酒 |
| `metal_write_sheet` | `MetalPlugin.metal_write_sheet` | 有色金属 |
| `chemical_write_sheet` | `ChemicalPlugin.chemical_write_sheet` | 化工 |
| `pesticide_write_sheet` | `PesticidePlugin.pesticide_write_sheet` | 农药 |
| `pesticide_write_summary` | `PesticidePlugin.pesticide_write_summary` | 农药 |


**Pipeline 执行流程：**

```
run()
  ├── 1. DataReader(source_file)            — 预加载源数据到内存（自带缓存）
  ├── 2. _create_backup()                   — 复制目标模板到 output_dir（带时间戳）
  ├── 3. openpyxl.load_workbook(backup)     — 在内存中打开副本
  ├── 4. for sheet_config in sheets:        — 逐 sheet 处理
  │     ├── 构建 context = {wb, ws, data_reader, sheet_config, defaults}
  │     ├── for action in actions:          — 执行主要处理动作
  │     │     └── registry[action['type']](context, {**defaults, **action})
  │     └── for post in post_processes:     — 执行后处理动作
  │           └── registry[post['action']](context, post['params'])
  ├── 5. wb.save(backup_file)               — 保存文件
  └── 6. for finalize in finalize_actions:  — 执行文件级后处理（如 XML 修复）
        └── registry[finalize['action']]({filepath, config}, finalize['params'])
```

---

### 1.2 DataReader（[core_engine/data_reader.py](core_engine/data_reader.py)）

| 项目 | 说明 |
|------|------|
| **功能** | 带缓存的 Excel 数据读取器。验证文件存在性和格式有效性，读取源 Excel 工作表的全量数据并缓存到内存，避免重复 I/O。提供按指标代码（AA 码）精确提取数据的功能。 |
| **入参** | `file_path: str` — 源 Excel 文件路径 |
| **出参** | 实例化后提供两个核心方法返回数据 |
| **可复用范围** | 所有行业通用，作为数据读取的标准入口 |
| **依赖限制** | 依赖 pandas、openpyxl；仅支持 .xlsx/.xls 格式 |

**核心方法：**

| 方法 | 功能 | 入参 | 出参 |
|------|------|------|------|
| `read_sheet_data(sheet_name)` | 读取并缓存整个工作表数据（全量，无表头） | `sheet_name: str` | `pd.DataFrame` |
| `read_indicator_data(sheet_name, indicator_code)` | 读取指定工作表中特定 AA 码指标的数据，自动定位指标所在列并提取日期和数值 | `sheet_name: str`, `indicator_code: str` | `Dict{"metadata": Dict, "data": pd.DataFrame}` |

---

### 1.3 DataProcessor（[core_engine/data_processor.py](core_engine/data_processor.py)）

| 项目 | 说明 |
|------|------|
| **功能** | 基于 Pandas 的数据处理工具集。提供日期筛选、排序、透视表创建、同比计算、季度和 YTD 统计、多指标合并、同比差值/同比19差值公式注入等功能。 |
| **入参** | `data_reader: DataReader` — DataReader 实例 |
| **出参** | 按具体方法返回处理后的数据或 None |
| **可复用范围** | 所有行业通用，各 plugin 内部可调用 |
| **依赖限制** | 依赖 pandas、numpy、openpyxl |

**核心方法一览：**

| 方法 | 功能说明 | 入参 | 出参 |
|------|---------|------|------|
| `filter_by_date_range(indicator_data, start_date, end_date)` | 按日期范围筛选指标数据 | 指标数据字典、起止日期 | 筛选后的数据字典 |
| `sort_by_date(indicator_data, ascending=True)` | 按日期排序 | 指标数据字典、升/降序 | 排序后的数据字典 |
| `create_month_year_pivot(indicator_data, value_col)` | 创建月份×年份交叉透视表 | 指标数据字典、值列名 | `pd.DataFrame` |
| `calculate_year_over_year_change(indicator_data)` | 计算同比变化率 | 指标数据字典 | 含同比变化率列的数据字典 |
| `add_quarterly_stats(ws, start_row, data_df, value_cols)` | 向工作表添加季度汇总统计（自动分组求和/求均值），同时计算季度同比 | ws、起始行、数据DataFrame、值列列表 | `(季标题行号, 季度结果列表)` |
| `add_ytd_stats(ws, last_quarter_row, data_df, value_cols)` | 添加年初至今（YTD）汇总统计、全年统计，含同比上年和同比2019 | ws、末季行号、数据DataFrame、值列列表 | `int`（最后一行的行号） |
| `merge_indicators(indicators, on="日期")` | 将多个指标数据按日期外连接合并为一个 DataFrame | 指标数据字典列表、连接键 | `pd.DataFrame` |
| `apply_yoy_diff_formulas(backup_file, sheet_name, yoy_diff_cols, start_row)` | 向备份文件指定列写入同比差值公式（含季度/YTD/全年特殊处理） | 文件路径、sheet名、列列表、起始行 | None |
| `apply_diff19_formulas(backup_file, sheet_name, diff19_cols, start_row)` | 向备份文件指定列写入与2019年同期的差值公式 | 文件路径、sheet名、列列表、起始行 | None |

**`add_quarterly_stats` 特殊处理逻辑：**
- 对 `AL`、`AO`、`AR`、`AU` 四列计算季度**平均值**（而非累加）
- 对其他列（B、E、H、K、N、Q、T、W、Z、AC、AF、AI、AX）计算季度**累加值**
- 自动计算季度同比（下一列写入）
- 不同年份间插入空行分隔

**`add_ytd_stats` 特殊处理逻辑：**
- YTD 标题行使用公式 `="YTD"`，高亮显示月份数
- 查找每年第一个月行号作为基准行，用 `SUM(基准行:基准行+N-1)` 或 `AVERAGE(...)` 生成 YTD 公式
- `AL/AO/AR/AU` 列使用 AVERAGE，其他列使用 SUM
- 自动生成全年汇总区（需12个月完整数据）
- 为 YTD 和全年数据计算同比上年、同比2019

---

### 1.4 transformers（[core_engine/transformers.py](core_engine/transformers.py)）

| 项目 | 说明 |
|------|------|
| **功能** | Excel 公式注入器。向指定工作表的目标列写入同比增长公式（`=当期/上年同期-1`）。支持年度数据、季度数据、月度数据的不同偏移逻辑。 |
| **入参** | `apply_formula(ws, config)` — ws 为目标工作表，config 包含 `target_cols`、`formula_type`、`start_row`、`end_row`、`params` 等 |
| **出参** | 无返回值，直接在 ws 上写入公式 |
| **可复用范围** | 通用，但主要用于简单场景，行业复杂公式由各 plugin 自行实现 |
| **依赖限制** | 依赖 `core_engine/utils.py` 的列字母转换工具 |
| **支持的 formula_type** | `yoy`（同比：`=当期/上年同期-1`）、`custom2`（同比2019，为预留接口） |

---

### 1.5 utils（[core_engine/utils.py](core_engine/utils.py)）

| 项目 | 说明 |
|------|------|
| **功能** | Excel 列字母与数字索引的双向转换工具函数 |
| **入参/出参** | `column_letter_to_number(letter: str) -> int`；`column_number_to_letter(number: int) -> str` |
| **可复用范围** | 所有行业通用，被多个模块引用（`transformers.py`、`data_processor.py`、各 plugin） |
| **依赖限制** | 无外部依赖，纯算法实现 |

---

## 二、各行业特殊处理逻辑文档

---

### 2.1 航空行业（[plugins/aviation_plugin.py](plugins/aviation_plugin.py)）

#### 整体处理流程
1. 逐个航空公司 sheet 写入基础指标数据（含单位转换）→ 自动生成季度和 YTD 统计
2. 计算同比上年增长率（yoy）→ 计算同比上年差值（yoy_diff）
3. 计算同比2019增长率（yoy19）→ 计算与2019差值（diff19）
4. 后处理：清理早年公式 → 调整全年行后格式 → 清除YTD 2018差值 → 清除特殊列
5. 月度/季度汇总表写入标题和 INDIRECT 跨表引用公式

#### 核心处理函数

**`aviation_write_airline_sheet(context, params)`**
- **功能**：写入航空公司基础指标数据。以第一个指标日期为基准，自动补全起始月份到首个数据月份之间的缺失日期（月末日期），写入日期列和各指标列，支持单位转换。调用 `DataProcessor.add_quarterly_stats()` 和 `add_ytd_stats()` 生成季度/YTD/全年统计。
- **特殊逻辑**：
  - 自动补全日期（以月末日期填充缺失月份）
  - 单位转换：春秋/吉祥 ×0.01，国泰 ×0.001
  - 国泰航空缺失值派生：`H = B - E`，`Q = K - N`
  - AL/AO/AR/AU 四列季度统计使用均值（非累加），格式保留两位小数
- **依赖**：`DataProcessor.add_quarterly_stats()`、`DataProcessor.add_ytd_stats()`

**`aviation_apply_yoy_formulas(context, params)`**
- **功能**：计算同比上年增长率（`=当期/上年同期-1`）。根据 A 列标签类型自动匹配偏移行数：年偏移2行、Q（不含18年）偏移5行、18Q使用2017年月度SUM公式、月度偏移12行、全年后年偏移1行。
- **特殊逻辑**：识别"年"、"Q"、"2017-"等多种日期标签格式，2018年（含18Q）不计算同比；prev_cell 为空时跳过公式写入

**`aviation_apply_yoy_diff_formulas(context, params)`**
- **功能**：计算同比上年差值（`=当期-上年同期`）。逻辑同增长率但为减法计算。18Q特殊处理：`=当期-AVERAGE(2017年对应季度三个月)`。

**`aviation_apply_yoy19_formulas(context, params)`**
- **功能**：计算相对于2019年同期的增长率（`=当期/2019同期-1`）。定位2019年各季度行（19Q1~Q4）、2019年全年行、全年后2019年行，按标签类型匹配对应行号。
- **特殊逻辑**：2023年及以后数据才计算；全年行需区分"全年"前后两个2019年行位置

**`aviation_apply_diff19_formulas(context, params)`**
- **功能**：计算相对于2019年同期的差值（`=当期-2019同期`）。逻辑同 yoy19 但为减法。

**`aviation_clear_early_years_data(context, params)`**
- **功能**：清除2014-2017年早年的同比/同比2019列公式，保持空白。

**`aviation_adjust_format_after_full_year(context, params)`**
- **功能**：全年行以下的同比/同比19列数据格式调整为一位小数（`0.0%`）。

**`aviation_clear_ytd_2018_diff_data(context, params)`**
- **功能**：清除YTD区域中2018年的同比差值数据。

**`aviation_clear_ax_ay_az_columns(context, params)`**
- **功能**：清除季度/YTD/全年区域中AX、AY、AZ三列的残留公式，防止`#NAME?`等错误。

**`aviation_write_monthly_report_header_info(context, params)`**
- **功能**：处理月度汇总表。从各航空公司子表获取最新日期，统一后写入汇总表标题（"【申万交运】XXXX年XX月航空公司数据汇总"），写入"当期值/同比YYYY-1/同比2019"参数列。
- **特殊逻辑**：多个工作表最新日期不一致时取最小值；将最新行号临时存入C1供后续 `aviation_write_report_data` 使用。

**`aviation_write_quarterly_report_header_info(context, params)`**
- **功能**：处理季度汇总表。从各航空公司子表获取最新季度标识（如25Q1），写入标题和"当期值/同比YY-1Qn/同比19Qn"参数列。
- **特殊逻辑**：季度按文本格式"yyQn"解析，年份自动补全为20yy；所有工作表季度必须一致，否则报错。

**`aviation_write_report_data(context, params)`**
- **功能**：在汇总表中写入 INDIRECT 跨表引用公式，从各航空公司子表自动汇总数据到总表。根据第1行表头定位目标表列号，逐行生成 `=@INDIRECT("'"&col$1&"'!源列目标行")` 公式。
- **特殊逻辑**：目标行号从C1临时值读取（由前置标题函数写入）；写入完成后清空C1。

#### 航空数据列结构

```
A列: 日期/标签       B~AU列: 17个指标原值      C/D...: 同比/同比19/差值交替
                                                
A列标签格式:          原始列(B, E, H, K, N, Q, T, W, Z, AC, AF, AI, AL, AO, AR, AU, AX)
2014-01-31              ↑ 17个航空公司运营指标
...                     
季度                     同比列 = 原始列 + 1 (C, F, I, ...)
YTD                      同比19列 = 原始列 + 2 (D, G, J, ...)
全年                      差值列 = 同比列 + 11 (AM, AP, AS, AV)
                         差值19列 = 差值列 + 1 (AN, AQ, AT, AW)
```

---

### 2.2 宏观行业（[plugins/macro_plugin.py](plugins/macro_plugin.py)）

#### 整体处理流程
1. 写入指标组数据（日期 + 多指标列，最新日期在前）
2. 创建基础透视表（月-日为行、年份为列）
3. 生成春节标签透视表 / 除夕相对天数透视表 / 周度透视表
4. 生成按年日期脚手架

#### 核心处理函数

**`macro_write_indicator_group(context, params)`**
- **功能**：写入一组指标数据到目标工作表。以第一个指标日期为基准，后续指标依次写入相邻列。
- **入参**：`indicators`（AA码列表）、`start_row`、`start_col`、`date_format`、`ascending`
- **特殊逻辑**：默认降序排列（最新日期在前），指标值0自动转为None

**`macro_create_pivot_table(context, params)`**
- **功能**：创建基础透视表，按"月-日"为行、年份为列进行透视。
- **特殊逻辑**：自动将闰年2月29日映射到2月28日；仅包含最近N年（默认7年）数据；空值用0填充

**`macro_create_festival_pivot(context, params)`**
- **功能**：生成基于春节标签的透视表。从工作表读取日期和数值数据，结合农历日期计算春节相对周数（节前N周 / 春节当周 / 节后N周），按标签和年份透视。
- **特殊逻辑**：使用 `add_global_spring_festival_tags()` 计算相对周数；使用 `generate_lunar_table_with_tags()` 生成农历对照表（依赖 ZhDate 库）

**`macro_create_chuxi_pivot(context, params)`**
- **功能**：生成基于除夕相对天数的透视表。以除夕当天为"t"，前30天"t-N"至后60天"t+N"为行、年份为列。
- **特殊逻辑**：处理跨年日期；除夕日期通过农历计算（腊月三十或廿九）

**`macro_create_weekly_pivot(context, params)`**
- **功能**：生成按ISO周统计的透视表。从工作表提取ISO年份和周数，按周数×年份透视。

**`macro_create_yearly_date_scaffold(context, params)`**
- **功能**：生成按年分列的日期脚手架（每天一行），每年占用两列（日期 + 预留数值列）。

---

### 2.3 银行行业（[plugins/bank_plugin.py](plugins/bank_plugin.py)）


#### 核心处理函数

**`bank_write_data(context, params)`**
- **功能**：处理各省份不良率数据。从 data_reader 读取多个指标，以第一个指标日期为基准，筛选季度月份（3、6、9、12月）数据，按日期对齐写入。
- **特殊逻辑**：仅保留季度末月份数据；继承目标列首行的源格式

**`bank_commercial_write_data(context, params)`**
- **功能**：处理商业银行关键监管指标数据。支持多列日期写入（`date_columns` 参数），支持从指定行读取单位转换因子。
- **特殊逻辑**：多列日期写入（各数据区域独立日期列）；单位转换因子从工作表指定行读取；日期升序排列

**`bank_commercial_formula(context, params)`**
- **功能**：通用公式写入函数。支持自定义公式模板和占位符替换，逐行写入直到前一列无数据为止。
- **支持的占位符**：
  - `{source_column}` → 源列字母
  - `{row}` → 当前行号
  - `{row-N}` → 当前行号偏移N
  - `{tname}` → 工作表名称（跨表引用）
- **常用公式模板示例**：
  - `={source_column}{row}/100` — 值转为百分比
  - `=IF(ISBLANK({source_column}{row}),"",{source_column}{row}/100)` — 空值保护百分比
  - `={source_column}{row}-{source_column}{row-1}` — 环比差值
  - `={source_column}{row}-{source_column}{row-4}` — 同比差值（季度数据）
  - `={source_column}{row}/{source_column}{row-4}-1` — 同比上年增长（季度数据）
  - `=IF(OR(ISBLANK(...),ISBLANK(...)),"",({source_column}{row}-{source_column}{row-4})*100)` — 带空值保护的BPS差值
  - `=IF(MONTH(B{row})=3,{source_column}{row},{source_column}{row}-{source_column}{row-1})` — 季度累积逻辑

#### 银行特殊格式与公式一览（按 sheet）

| sheet 名称 | 公式类型 | 功能 |
|-----------|---------|------|
| 总资产 | `=col/100` | 值转百分比（T-Y列） |
| 资产增速及增量 | `=IF(ISBLANK,col/100,"")` | 空值保护百分比 |
| 资产增速及增量 | `={tname}!col-row - {tname}!col-(row-1)` | 跨表环比增量 |
| 净息差 | `=(col-row - col-(row-1))*100` | 环比BPS差值 |
| 净息差 | `=(col-row - col-(row-4))*100` | 同比BPS（季） |
| 净利润 | `=col-row/col-(row-4)-1` | 同比上年增长（季） |
| 净利润 | `=IF(MONTH(Brow)=3,col-row,col-row-col-(row-1))` | 季度累积逻辑 |
| 不良率 | `=(col-row - col-(row-4))*100` | 同比BPS（半年数据） |
| 拨备覆盖率 | `=col-row-col-(row-1)` | 环比差值（pct单位） |
| 资本 | `=col/100` | 值转百分比 |

---

### 2.4 医疗健康行业（[plugins/medical_plugin.py](plugins/medical_plugin.py)）

#### 数据格式转换说明

```
源格式（纵向）：                    目标格式（横向）：
日期        | 指标值                 A列: 指标名称   B列起: 2020.1 | 2020.2 | ...
2020-01-01 | 6.36                   第4行: 国内医疗   | 6.36   | 2.56   | ...
2020-02-01 | 2.56                   第5行: 同比公式
```

#### 核心处理函数

**`medical_write_data(context, params)`**
- **功能**：横向写入医疗数据。从所有指标源数据提取统一时间周期序列，将时间周期作为列头写入（格式 "YYYY.M"），各指标数据按日期对号写入对应列。
- **特殊逻辑**：自动解除目标行的合并单元格后写入；合并所有指标日期的并集作为统一列头；按 `(year, month)` 元组精确匹配

**`medical_write_quarter(context, params)`**
- **功能**：自动判断季度边界，写入合并季度列头（如 `2025Q1`）。
- **特殊逻辑**：根据月份计算季度 `(month-1)//3+1`；按列连续性分组同季度月份；自动合并同季度月份单元格；跨年度自动切分

**`medical_write_formula(context, params)`**
- **功能**：横向写入公式。从起始列向右遍历写入，直到日期行最后一个非空列。
- **支持的占位符**：`{col}`（当前列字母）、`{col-N}`（左偏移N列的字母）、`{row}`（当前行号）、`{source_row}`（源数据行号）
- **常用公式**：
  - 同比：`=IF(OR(ISBLANK({col}{source_row}),ISBLANK({col-12}{source_row})),"",({col}{source_row}-{col-12}{source_row})/{col-12}{source_row})`
  - 环比：`=IF(OR(ISBLANK({col}{source_row}),ISBLANK({col-1}{source_row})),"",({col}{source_row}-{col-1}{source_row})/{col-1}{source_row})`

**`medical_merge_quarter_data(context, params)`**
- **功能**：将同季度内各月份的数据合并到该季度起始列，清理其他列。
- **特殊逻辑**：优先取季度末月份值（Q1→3月、Q2→6月、Q3→9月、Q4→12月）；找不到则取该季度最后一列有值的；处理合并单元格的只读保护（MergedCell skip）

---

### 2.5 电子行业（[plugins/electronics_plugin.py](plugins/electronics_plugin.py)）

#### 整体处理流程
1. 分区域（section）写入各存储产品价格数据（每个区域独立日期列）
2. 更新图表引用范围为最近两年

#### 核心处理函数

**`electronics_write_sheet(context, params)`**
- **功能**：写入多区域工作表。每个区域具有独立的日期列和不同的指标集合，支持日度和周度数据。按AA代码去重加载数据，合并所有指标日期并集，降序排列（最新在前）。
- **特殊逻辑**：自动按最大值列去重；自动识别自定义格式列（`custom_formats`）；多区域独立日期列

**`electronics_update_chart_ranges(context, params)`**
- **功能**：更新Excel图表数据引用范围，自动截断为最近两年数据。
- **特殊逻辑**：解析现有图表系列引用公式 → 查找最新日期 → 向下查找两年前截止行 → 统一重写所有series的值和类别范围；清除numCache强制图表刷新

#### 各区域配置

| 工作表 | 区域 | 产品类型 | 指标数 |
|-------|------|---------|--------|
| 现货价格 | DRAM | DDR5/DDR4/DDR3颗粒 | 7 |
| 现货价格 | NAND Wafer | 128G~1Tb TLC | 4 |
| 现货价格 | NAND Flash | MLC/SLC各容量 | 9 |
| 存储器价格指数（周） | — | DRAM/NAND指数 | 2 |
| 存储产品价格_周 | 存储器指数 / NAND晶圆 / SSD / eMMC / DDR / 内存条 / LPDDR | 7个区域各若干指标 | 32 |

---

### 2.6 公用事业行业（[plugins/public_plugin.py](plugins/public_plugin.py)）

#### 核心处理函数

**`public_write_data(context, params)`**
- **功能**：处理公用事业基础指标数据，写入目标工作表。支持从指定行读取单位转换因子。
- **特殊逻辑**：日期按**降序**排列（最新在前），与航空/银行的升序不同；单位转换因子从工作表指定行读取（`unit_conversion_row`）

**`public_elec_write_title(context, params)`**
- **功能**：写入电力报表的动态标题。根据最新日期自动计算并格式化主标题（当月）和副标题（上月）。
- **特殊逻辑**：2月份标题显示为"1-2"（合并1-2月数据）；处理上年12月→本年1月的年份切换

**`public_t3_write_header(context, params)`**
- **功能**：写入T+3（三日滚动）报表的表头日期。根据最新日期和其下一行日期写入两个表头单元格。

---

### 2.7 白酒行业（[plugins/baijiu_plugin.py](plugins/baijiu_plugin.py)）

#### 整体处理流程
1. 按 AA 指标代码匹配写入数据（`baijiu_write_sheet_by_code`）
2. 或按产品名模糊匹配写入（`baijiu_write_sheet`）
3. 填充汇总行（计算最新/上周/上月/年初值）
4. 填充"作图"sheet 的公式 → 实算值
5. finalize：直接操作 ZIP 修复图表 XML（图表范围更新、模板图像恢复）

#### 核心处理函数

**`baijiu_write_sheet_by_code(context, params)`**
- **功能**：按AA指标代码精确匹配写入数据。合并所有指标日期并集，统一升序写入日期列和指标值。
- **入参**：`indicators`（AA码→目标列映射）、`data_start_row`、`date_col`
- **特殊逻辑**：全量读取后用 sort + reset_index 统一处理

**`baijiu_write_sheet(context, params)`**
- **功能**：按产品名匹配写入数据。从源文件读取产品名列（第0行含 `brand:product` 格式），解析产品名和品牌；与目标表头做精确匹配 → 模糊匹配。
- **入参**：`manual_map`（手动映射词典）、`data_start_row`、`date_col`、`header_read_row`
- **特殊逻辑**：三层匹配策略：手动映射 → 精确名称匹配 → 子串包含匹配

**`baijiu_fill_summary_rows(context, params)`**
- **功能**：填充数据sheet顶部的汇总行（第1~4行）。计算最新日期值、7天前值、28天前值、364天前值，写入对应的实际数值（取代原VLOOKUP公式，因openpyxl不计算公式）。
- **特殊逻辑**：对每个数据列，从日期列向下扫描找到 ≤ 目标日期的最近一条有效数据

**`baijiu_fill_zuotu(context, params)`**
- **功能**：填充"作图"sheet的公式值为实算值。解析各种Excel公式（VLOOKUP、跨表引用、MAX、简单算术），递归求解后直接写入数值。
- **特殊逻辑**：支持 VLOOKUP（含近似匹配）、MAX区域、跨sheet引用、减法运算；结果设置 `0.00` 或 `yyyy-mm-dd` 格式

**`baijiu_finalize_charts(context, params)`**
- **功能**：通过ZIP操作修复被openpyxl破坏的图表XML。从原始模板恢复 drawing/vml/theme 文件，修补图表系列的引用范围（截断为最近数据范围），清除缓存标签强制刷新。
- **特殊逻辑**：
  - 从原始模板文件恢复 `xl/drawings/`、`xl/ctrlProps/`、`xl/theme/` 下的文件
  - 跳过 openpyxl 错误命名的 `commentsDrawing*.vml`
  - 每个图表根据 sheet 索引和数据列范围计算正确的系列引用
  - 前两个 sheet（各酒厂主要产品）的图表从最近两年数据开始
  - 自动设置 X 轴主单位=365天、次单位=30天
  - 清除 `dLbls`、`numCache`、`strCache` 标签防止旧缓存

**`_CHART_SHEET_MAP`** 映射表：维护49个图表到其数据源 sheet 索引的对应关系。

---

### 2.8 有色金属行业（[plugins/metal_plugin.py](plugins/metal_plugin.py)）

#### 整体处理流程
单个通用函数完成所有sheet的数据写入：从源文件读取 → 匹配AA码 → 日期降序写入 → 指标数据对齐写入。

#### 核心处理函数

**`metal_write_sheet(context, params)`**
- **功能**：从源文件读取全量数据（源文件结构：第2行为AA码行，第11行起为数据），建立AA码→源列索引映射，将数据按日期降序写入目标对应列。
- **入参**：`source_sheet`、`data_start_row`、`date_col`、`indicators`（AA码→目标列映射）
- **特殊逻辑**：
  - 源数据固定从第11行开始读取（row index 10）
  - AA码从源文件第2行（row index 1）读取
  - 日期降序排列（最新在前）
  - 数值格式统一为 `0.00`

---

### 2.9 化工行业（[plugins/chemical_plugin.py](plugins/chemical_plugin.py)）

#### 整体处理流程
根据目标工作表的列头自动推断每个指标的日期列位置（向左扫描找到"日期"列头），支持一个sheet有多组独立的日期列和数据列。

#### 核心处理函数

**`chemical_write_sheet(context, params)`**
- **功能**：按AA指标代码读取源数据，按日期列分组写入。自动向左扫描目标表头行找到最近的"日期"列号，同一日期列的多个指标共享日期序列。
- **入参**：`source_sheet`、`data_start_row`、`indicators`（AA码→目标列映射）
- **特殊逻辑**：
  - 每个指标的日期列通过 `_find_indicator_date_column()` 向左扫描表头自动判定
  - 按日期列分组：同一日期列的所有指标共享日期序列
  - 每组日期列独立按降序写入
  - 数值格式统一为 `0.00`

---

### 2.10 农药行业（[plugins/pesticide_plugin.py](plugins/pesticide_plugin.py)）

#### 整体处理流程
1. 按AA指标代码匹配写入各品类价格数据（价格指数、除草剂、杀虫剂、杀菌剂、中间体）
2. 汇总sheet填充：季度均价、年度均价、年初价格、月末价格、季末价格

#### 核心处理函数

**`pesticide_write_sheet(context, params)`**
- **功能**：收集所有指标的日期→值映射，合并日期并集，按日期**升序**写入（最早→最新）。
- **入参**：`source_sheet`、`data_start_row`、`date_col`、`indicators`（AA码→目标列映射）
- **特殊逻辑**：日期升序排列（与金属/化工的降序不同）；按日期并集统一对齐所有指标

**`pesticide_write_summary(context, params)`**
- **功能**：从数据sheet（如"除草剂"）读取所有品种的日期→值数据，计算并写入汇总sheet的五个区块：
  - **季度均价**（12列）：最近12个完整季度的均价
  - **年度均价**（7列）：最近7个完整年度的均价
  - **年初价格**（1列）：最近完整年份的第一个数据
  - **月末价格**（1列）：最近完整月份的最后一个数据
  - **季末价格**（1列）：最近完整季度的最后一个数据
- **入参**：`source_data_sheet`（数据源sheet名）、`data_start_row`
- **特殊逻辑**：
  - 自动确定"最近完整季度" = 最新数据所在季度的上一个季度
  - 自动确定"最近完整年份" = 最新数据年份 - 1
  - 自动清除溢出列的旧标签和旧数据（季度列12列后、年度列7列后）
  - 所有价格保留4位小数后四舍五入

---

## 三、各行业已开发 Sheet 清单

---

### 3.1 交运-航空（[configs/db_aviation.yaml](configs/db_aviation.yaml)）— 9个 Sheet

| 序号 | Sheet 名称 | 类型 | 说明 |
|------|-----------|------|------|
| 1 | 中国国航 | 航空公司数据 | 7家航司之一，17个AA指标→B~AX列 |
| 2 | 中国东航 | 航空公司数据 | 7家航司之一，17个AA指标 |
| 3 | 南方航空 | 航空公司数据 | 7家航司之一，17个AA指标 |
| 4 | 春秋航空 | 航空公司数据 | 含单位转换(×0.01)，17个AA指标 |
| 5 | 吉祥航空 | 航空公司数据 | 含单位转换(×0.01)，17个AA指标 |
| 6 | 海航控股 | 航空公司数据 | 7家航司之一，17个AA指标 |
| 7 | 国泰航空 | 航空公司数据 | 含单位转换(×0.001)，11个AA指标，自动派生H/Q列 |
| 8 | 月度汇总表 | 跨表汇总 | 从6家国内航司汇总月度数据（INDIRECT公式），含同比/同比19行 |
| 9 | 季度汇总表 | 跨表汇总 | 从6家国内航司汇总季度数据（INDIRECT公式），含同比/同比19行 |

**航空 Sheet 通用结构：** 每个航司Sheet包含 月度数据区（B~AZ列，约150+行）→ 季度汇总区（含同比列）→ YTD汇总区（含同比/同比19列）→ 全年汇总区（含同比/同比19列）。支持同比上年增长率、同比上年差值、同比2019增长率、与2019差值四种公式计算。后处理清理早年无意义公式并调整格式。

---

### 3.2 宏观行业（[configs/db_macro.yaml](configs/db_macro.yaml)）— 30个 Sheet

| 序号 | Sheet 名称 | 行业分类 | 主要处理逻辑 |
|------|-----------|---------|------------|
| 1 | 1.1 电力 | 能源 | 指标组写入 + 透视表 + 春节标签透视表 |
| 2 | 1.2 焦炭、铁矿石 | 材料 | 指标组写入 + 透视表 + 春节标签透视表 |
| 3 | 1.2 钢铁 | 材料 | 多指标组 + 多透视表 + 春节标签透视表 |
| 4 | 1.2 螺纹钢 | 材料 | 指标组 + 透视表 + 春节标签透视表 |
| 5 | 1.2 冷轧板卷 | 材料 | 指标组 + 透视表 + 春节标签透视表 |
| 6 | 1.2 中厚板 | 材料 | 指标组 + 透视表 + 春节标签透视表 |
| 7 | 1.4 纯碱、橡塑 | 化工 | 多指标组 + 多透视表 + 春节标签透视表 |
| 8 | 1.5 化纤 | 化工 | 多指标组 + 多透视表 + 春节标签透视表 |
| 9 | 1.6 汽车 | 汽车 | 指标组 + 透视表 + 春节标签透视表 |
| 10 | 1.5 新能源 | 能源 | 指标组 + 透视表 + 春节标签透视表 |
| 11 | 2.4 (月度)基建数据 | 基建 | 指标组 + 透视表 |
| 12 | 3.1 货运流量 | 物流 | 指标组 + 透视表 + 春节标签透视表 |
| 13 | 3.1 省份货运流量 | 物流 | 指标组写入（25个省份） |
| 14 | 3.1 交通运输部物流数据 | 物流 | 多指标组 + 多透视表 + 春节标签透视表 |
| 15 | 3.1 交通运输部物流(月度) | 物流 | 指标组 + 多透视表 |
| 16 | 3.2 城市间 | 交通 | 指标组 + 日期脚手架 + 除夕透视表 |
| 17 | 3.4 干散货 | 航运 | 指标组 + 透视表 |
| 18 | 3.4 集装箱运价 | 航运 | 指标组（13项）+ 13个透视表 |
| 19 | 3.4 出口（集装箱吞吐） | 贸易 | 指标组写入 |
| 20 | 3.4 出口(陆路) | 贸易 | 多指标组 + 多透视表 + 春节标签透视表 |
| 21 | 4.1 新房成交 | 房地产 | 指标组 + 日期脚手架 + 除夕透视表 |
| 22 | 4.1 新房成交(一线城市) | 房地产 | 指标组 + 透视表（4个城市） |
| 23 | 4.1 新房成交(二线城市iFind) | 房地产 | 指标组（10城市）+ 多日期脚手架 |
| 24 | 4.2 二手房挂牌价格 | 房地产 | 指标组 + 5个透视表 |
| 25 | 4.2 二手房挂牌量 | 房地产 | 指标组 + 5个透视表 |
| 26 | 4.3 土地 | 房地产 | 指标组（12个）+ 12个透视表 |
| 27 | 5.1 农产品物价指数 | 农业 | 指标组 + 5个透视表 |
| 28 | 5.2 工业品价格指数(日度) | 工业 | 指标组（8个）+ 8个透视表 |
| 29 | 5.2 工业品价格指数(金属类) | 工业 | 指标组（4个）+ 4个透视表 |
| 30 | 5.2 五金机电价格指数(周度) | 工业 | 指标组（11个）+ 4个透视表 |

注：宏观含约60个AA指标，覆盖电力、焦炭、钢铁、化纤、汽车、物流、房地产、农业、工业品等领域。支持的透视表类型：基础透视表（月-日×年份）、春节标签透视表（节前6周~节后12周）、除夕相对天数透视表（t-30~t+60）、周度透视表、按年日期脚手架。

---

### 3.3 银行-商业银行关键监管指标（[configs/db_bank_commercial.yaml](configs/db_bank_commercial.yaml)）— 10个 Sheet


| 序号 | Sheet 名称 | 指标数 | 数据处理动作 |
|------|-----------|--------|-------------|
| 1 | 总资产 | 12 | 写入数据 + 公式(值/100) |
| 2 | 资产增速及增量 | 7 | 写入数据 + 百分比公式 + 跨表环比增量 |
| 3 | 净息差 | 7 | 写入数据 + 百分比公式 + 环比BPS + 同比BPS |
| 4 | 净利润 | 7 | 写入数据 + 同比公式 + 季度累积 + 同比百分比 |
| 5 | 资产利润率(ROA) | 7 | 写入数据 + 百分比公式 + 环比BPS |
| 6 | 资本利润率(ROE) | 1 | 写入数据 + 百分比公式 + 环比BPS + 同比BPS |
| 7 | 非利息收入占比 | 1 | 写入数据 + 百分比公式 + 环比BPS + 同比BPS |
| 8 | 不良率 | 5 | 写入数据 + 百分比公式 + 同比BPS |
| 9 | 拨备覆盖率 | 7 | 写入数据 + 百分比公式 + 环比差值 + 同比差值 |
| 10 | 资本 | 7 | 写入数据 + 百分比公式 + 环比BPS |

注：共约61个AA指标，覆盖总资产、净息差、净利润、ROA、ROE、不良率、拨备覆盖率、资本充足率等商业银行关键指标。每个Sheet按银行类型（大型/股份/城商/农商/外资/民营）分列。

---

### 3.4 银行-各省不良率（configs/db_bank_bll.yaml）— 1个 Sheet

| 序号 | Sheet 名称 | 指标数 | 说明 |
|------|-----------|--------|------|
| 1 | 不良率 | 20 | 20个省份维度的不良率指标（仅保留3/6/9/12月季度数据） |

---

### 3.5 医疗健康（[configs/db_medical.yaml](configs/db_medical.yaml)）— 1个 Sheet


| 序号 | Sheet 名称 | 指标数 | 说明 |
|------|-----------|--------|------|
| 1 | sheet1 | 6 | 6个融资指标（横向格式），含同比/环比公式和季度合并 |

**指标行布局（横向）：**
- 第4行: 国内医疗健康融资额（数据行）
- 第5行: 融资额同比 / 第6行: 融资额环比
- 第7行: 国内医疗健康融资事件数
- 第8行: 事件数同比 / 第9行: 事件数环比
- 第10行: 国内医疗健康融资-早期投资
- 第11行: 早期投资同比 / 第12行: 早期投资环比
- 第13~15行: 季度合并行（融资额/同比/环比）
- 第16行: 海外医疗健康融资额
- 第17行: 海外融资同比 / 第18行: 海外融资环比
- 第25行: 医疗健康并购额
- 第26行: 并购额同比 / 第27行: 并购额环比

---

### 3.6 电子-存储价格（[configs/db_electronics.yaml](configs/db_electronics.yaml)）— 3个 Sheet

| 序号 | Sheet 名称 | 区域数 | 总指标数 | 说明 |
|------|-----------|--------|---------|------|
| 1 | 现货价格 | 3 | 20 | DRAM + NAND Wafer + NAND Flash 日度现货价 |
| 2 | 存储器价格指数(周) | 1 | 2 | DRAM/NAND 周度指数 |
| 3 | 存储产品价格_周 | 7 | 32 | 含存储器指数/NAND晶圆/SSD/eMMC/DDR/内存条/LPDDR |

注：共约54个AA指标。各区域独立日期列。自动更新图表引用范围为最近两年。

---

### 3.7 公用事业-环保（[configs/db_public_env.yaml](configs/db_public_env.yaml)）— 4个 Sheet

> ⚠️ 公用事业插件尚未集成到 PipelineEngine，但插件功能和配置文件均已完备。

| 序号 | Sheet 名称 | 指标数 | 说明 |
|------|-----------|--------|------|
| 1 | 发电数据 | 12 | 写入数据（含单位转换）+ 动态标题 |
| 2 | 用电数据 | 10 | 写入数据（含单位转换）+ 动态标题 |
| 3 | 表3 | 1 | 写入数据 + T+3表头日期 |
| 4 | 图6 | 2 | 写入原始数据 |

注：共约25个AA指标。发电/用电数据日期降序排列（最新在前）。单位转换因子从工作表指定行读取。

---

### 3.8 白酒-品牌价格（[configs/db_baijiu2.yaml](configs/db_baijiu2.yaml)）— 5个 Sheet + 49个图表

| 序号 | Sheet 名称 | 指标数 | 说明 |
|------|-----------|--------|------|
| 1 | 各酒厂主要产品 | 55 | 茅台、五粮液、泸州老窖等主要产品批价（按AA码匹配写入） |
| 2 | 飞天100ml 和 飞天1L原箱 | 2 | 飞天小瓶装和大瓶装价格 |
| 3 | 飞天次新酒 | 7 | 不同年份飞天次新酒价格 |
| 4 | 白酒品牌批价（今日酒价） | 60 | 多品牌批价（今日酒价数据源） |
| 5 | 白酒品牌批价 (酒价参考) | 57 | 多品牌批价（酒价参考数据源） |

**白酒特有处理：**
- `finalize_actions`：直接操作 ZIP 修复 49 个图表的 XML 系列引用，恢复被 openpyxl 破坏的 drawing/vml 文件
- 汇总行（第1~4行）自动填充最新/上周/上月/年初的实际值（取代VLOOKUP）
- "作图" sheet 的公式被解析为实算值

---

### 3.9 有色金属（[configs/db_metal.yaml](configs/db_metal.yaml)）— 5个 Sheet

| 序号 | Sheet 名称 | 指标数 | 说明 |
|------|-----------|--------|------|
| 1 | 图5金价与实际利率 | 2 | 金价 + 实际利率走势 |
| 2 | 图6美国通胀预期 | 2 | 通胀预期 + 实际利率 |
| 3 | 图7黄金ETF持仓 | 3 | 金价 + 2个ETF持仓指标 |
| 4 | 图8COMEX非商业持仓 | 2 | COMEX非商业多头/空头持仓 |
| 5 | 图10中国央行购金（月度） | 1 | 央行月度购金量 |

注：共约10个AA指标。数据从源文件第2行读取AA码，第11行起读取数据。日期降序排列（最新在前）。数值格式统一为 `0.00`。

---

### 3.10 化工（[configs/db_chemical.yaml](configs/db_chemical.yaml)）— 7个 Sheet 区域

| 序号 | Sheet 名称 | 指标数 | 说明 |
|------|-----------|--------|------|
| 1 | 原油与成品油 | 6 | 原油/成品油价格指标 |
| 2 | 石化衍生物（价格） | 9 | 石化衍生物各产品价格 |
| 3 | 石化衍生物（开工率） | 12 | 石化衍生物各产品开工率 |
| 4 | 石化烯烃与芳烃 | 9 | 烯烃与芳烃产品价格 |
| 5 | 聚氨酯（价格） | 9 | 聚氨酯各产品价格 |
| 6 | 聚氨酯（开工率） | 6 | 聚氨酯各产品开工率 |
| 7 | 农药 | 25 | 农药各产品价格（含国内/国外报价） |

注：共约76个AA指标。同一Sheet可能有多个独立日期列组（如价格区和开工率区各独立日期列）。每个指标的日期列通过向左扫描表头"日期"标签自动判定。日期降序排列。数值格式统一为 `0.00`。

---

### 3.11 农药（[configs/db_pesticide.yaml](configs/db_pesticide.yaml)）— 9个 Sheet

| 序号 | Sheet 名称 | 类型 | 指标数 | 说明 |
|------|-----------|------|--------|------|
| 1 | 价格指数 | 数据sheet | 4 | 农药整体价格指数 |
| 2 | 除草剂 | 数据sheet | 44 | 44个除草剂品种价格 |
| 3 | 杀虫剂 | 数据sheet | 40 | 40个杀虫剂品种价格 |
| 4 | 杀菌剂 | 数据sheet | 33 | 33个杀菌剂品种价格 |
| 5 | 中间体 | 数据sheet | 7 | 7个中间体品种价格 |
| 6 | 除草剂汇总 | 汇总sheet | — | 季度均价/年度均价/年初/月末/季末 |
| 7 | 杀虫剂汇总 | 汇总sheet | — | 季度均价/年度均价/年初/月末/季末 |
| 8 | 杀菌剂汇总 | 汇总sheet | — | 季度均价/年度均价/年初/月末/季末 |
| 9 | 中间体汇总 | 汇总sheet | — | 季度均价/年度均价/年初/月末/季末 |

注：共约128个AA指标（农药品类最全的数据集）。数据按日期升序排列（最早→最新）。汇总sheet自动计算最近12个季度均价、7个年度均价、年初价格、月末价格、季末价格。

---

## 附录 A：配置层入口脚本

| 脚本 | 对应YAML配置文件 | 行业 | 说明 |
|------|----------------|------|------|
| [run_aviation.py](run_aviation.py) | configs/db_aviation.yaml | 交运-航空 | 单行业入口 |
| [run_macro.py](run_macro.py) | configs/db_macro.yaml | 宏观 | 单行业入口 |
| [run_baijiu.py](run_baijiu.py) | configs/db_baijiu2.yaml | 白酒 | 单行业入口 |
| [run_metal.py](run_metal.py) | configs/db_metal.yaml | 有色金属 | 单行业入口 |
| [run_chemical.py](run_chemical.py) | configs/db_chemical.yaml | 化工 | 单行业入口 |
| [run_electronics.py](run_electronics.py) | configs/db_electronics.yaml | 电子 | 单行业入口 |
| [run_public.py](run_public.py) | 公用事业|  |
| [run_medical.py](run_medical.py) | configs/db_medical.yaml | 医疗 | 单行业入口  |
| [run_bank.py](run_bank.py) | configs/db_bank_commercial.yaml | 银行 | 单行业入口 |
| [run_pipeline.py](run_pipeline.py) | 命令行指定任意配置 | 通用 | `-c <config>` 单任务 / `--all` 全量 |

---

## 附录 B：项目目录结构

```
new_pipeline/
├── core_engine/                  # 核心引擎层（行业无关）
│   ├── action_dispatcher.py      # PipelineEngine：配置加载、动作注册与调度
│   ├── data_reader.py            # DataReader：带缓存的 Excel 数据读取
│   ├── data_processor.py         # DataProcessor：数据处理、季度/YTD统计、公式注入
│   ├── transformers.py           # 通用公式注入器（同比/环比）
│   └── utils.py                  # Excel列字母↔数字转换工具
├── plugins/                      # 行业插件层（各行业特殊逻辑）
│   ├── aviation_plugin.py        # 航空：12个动作
│   ├── macro_plugin.py           # 宏观：6个动作 + 春节/除夕/周度透视
│   ├── electronics_plugin.py     # 电子：2个动作（多区域 + 图表更新）
│   ├── baijiu_plugin.py          # 白酒：5个动作 + 49图表XML修复
│   ├── metal_plugin.py           # 有色金属：1个动作
│   ├── chemical_plugin.py        # 化工：1个动作（自动日期列检测）
│   ├── pesticide_plugin.py       # 农药：2个动作（数据写入 + 汇总计算）
│   ├── public_plugin.py          # 公用事业：3个动作
│   ├── bank_plugin.py            # 银行：3个动作
│   └── medical_plugin.py         # 医疗：4个动作
├── configs/                      # 配置层（YAML驱动）
│   ├── db_aviation.yaml          # 航空：9个Sheet
│   ├── db_macro.yaml             # 宏观：30个Sheet
│   ├── db_baijiu2.yaml           # 白酒：5个Sheet + finalize_actions
│   ├── db_metal.yaml             # 有色金属：5个Sheet
│   ├── db_chemical.yaml          # 化工：7个Sheet区域
│   ├── db_pesticide.yaml         # 农药：9个Sheet
│   ├── db_electronics.yaml       # 电子：3个Sheet
│   ├── db_public_env.yaml        # 公用事业：4个Sheet
│   ├── db_bank_commercial.yaml   # 银行商业：10个Sheet
│   ├── db_medical.yaml           # 医疗：1个Sheet
│   └── db_bank_bll.yaml          # 银行不良率：1个Sheet
├── run_pipeline.py               # 通用入口（支持 -c / --all）
├── run_aviation.py               # 航空专用入口
├── run_macro.py                  # 宏观专用入口
├── run_baijiu.py                 # 白酒专用入口
├── run_metal.py                  # 有色金属专用入口
├── run_chemical.py               # 化工专用入口
├── run_electronics.py            # 电子专用入口
├── run_public.py                 # 公用事业入口 
├── run_medical.py                # 医疗入口
├── run_bank.py                   # 银行入口
└── 配套文档.md                   # 本文档
```

---

## 附录 C：新行业接入检查清单

添加新行业时，需完成以下步骤：

1. **创建 Plugin**（`plugins/xxx_plugin.py`）
   - 定义带 `@staticmethod` 的处理函数
   - 签名统一为 `def action_name(context, params)`
   - `context` 包含 `wb`, `ws`, `data_reader`, `sheet_config`, `defaults`

2. **注册 Action**（`core_engine/action_dispatcher.py`）
   - 在文件顶部 `from plugins.xxx_plugin import XxxPlugin`
   - 在 `actions_registry` 中添加映射条目

3. **编写 YAML 配置**（`configs/db_xxx.yaml`）
   - 指定 `source_file`、`target_file`、`output_dir`
   - 定义 `defaults`（全局默认参数）
   - 定义 `sheets` 列表，每个 sheet 配置 `actions`（含 `type` 和参数）

4. **创建入口脚本**（`run_xxx.py`）
   - 标准模板：`PipelineEngine(config_path).run()`

5. **更新本文档**
   - 添加行业特殊处理逻辑（第二节）
   - 添加 Sheet 清单（第三节）
   - 更新 Action 注册表（第一节）
   - 更新目录结构和入口脚本表（附录）
